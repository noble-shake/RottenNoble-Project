# RottenNoble Frontend

React 19 + Vite. `.env.local`을 `.env.example` 기준으로 채운 뒤 아래 명령을 쓴다.

```bash
npm install
npm run dev      # 개발 서버
npm run build    # 프로덕션 빌드 (dist/)
npm run preview  # 빌드 결과 미리보기
npm run test     # Vitest
```
